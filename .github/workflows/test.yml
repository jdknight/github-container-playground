on: [push]

jobs:
  test:
    runs-on: windows-latest
    steps:
      - uses: ilammy/msvc-dev-cmd@v1
      - run: pip install releng-tool
      - run: |
          bash --version
          echo ""
          nmake /?
          echo ""
          hg --version
          echo ""
          perl --version
          echo ""
          releng-tool --version
          echo ""
          sed --version
      - run: env
      - run: bash -c "echo 123"
