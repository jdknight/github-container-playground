on: [push]

jobs:
  test:
    runs-on: windows-latest
    steps:
      - run: pip install releng-tool
      - run: |
          bash --version
          nmake /?
          perl --version
          releng-tool --version
          sed --version
